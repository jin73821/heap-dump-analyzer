# RAG 검색 품질 기준선

`python3 /opt/chroma/app/evaluate.py --k 10` 으로 재현한다.

## 2026-08-31 — 학습 코퍼스 84→204행 반영 / 956청크(485 문서)

`python3 /opt/chroma/app/evaluate.py --k 10` (앱과 **동일한 필터**: `synthetic` 제외 + `ai_chat` 제외)

| k | Recall@1 | Recall@k | MRR |
|---:|---:|---:|---:|
| 3 | 0.548 | 0.786 | 0.663 |
| 5 | 0.548 | 0.857 | 0.679 |
| **10** | 0.548 | **0.929** | 0.687 |
| 15 | 0.548 | 0.929 | 0.687 |
| 20 | 0.548 | 0.929 | 0.687 |

**k=10 권장 유지.** k=15 이상에서 더 오르지 않는다(남은 3건은 상위 20 밖).

소스별 Recall@1 / Recall@10:

| source | @1 | @10 |
|---|---:|---:|
| `oom_kind` | 7/8 | 8/8 |
| `csv` | 4/8 | 8/8 |
| `leak_lib` | 3/7 | 7/7 |
| `core_warn` | 4/6 | 6/6 |
| `mat_hint` | 5/5 | 5/5 |
| `vendor` | 0/3 | 3/3 |
| **`leak_fb`** | **0/5** | **2/5** |

남은 실패 3건은 전부 `leak_fb`(정규식 폴백 룰)이고, 밀어낸 문서가 **오답이라고 보기 어렵다**
— "Finalizer 큐가 밀립니다"에 `leak_fb:12`, "스레드가 계속 늘어납니다"에 `oom_kind:NATIVE_THREAD`,
"byte 배열이 힙 대부분"에 `ai_insight:11` 이 올라온다. 아래 *평가셋의 한계* 의 "정답을 1건으로
좁게 잡았다" 가 그대로 드러나는 자리다.

### 🔴 이 시점에 평가 방식이 바뀌었다 — 이전 수치와 직접 비교 금지

`evaluate.py` 가 `synthetic` 만 걸렀고 **앱이 검색에서 빼는 `ai_chat` 을 걸러 주지 않았다.**
즉 지금까지의 기준선은 **운영이 실제로 하지 않는 검색**을 잰 값이다(아래 2026-08-29 표 포함).
앱 필터(`ChromaSearchService.searchFilter`)와 같아지도록 고쳤고, 차이는 크다:

| 필터 | Recall@1 | Recall@10 | MRR |
|---|---:|---:|---:|
| 앱과 동일(`ai_chat` 제외) | 0.548 | **0.929** | 0.687 |
| 제외 없음(옛 방식, `--no-exclude`) | 0.429 | 0.833 | 0.582 |

⚠ **`evaluate.py` 의 `EXCLUDED_SOURCE_TYPES` 는 Java 쪽과 손으로 맞춘 사본이다.**
한쪽만 바꾸면 이 사고가 반복된다.

### 코퍼스 증가의 영향

학습 코퍼스가 84→204행(WAS 3종 150행 신규)으로 늘면서 **`csv` 는 8/8 을 유지**했고
`vendor`·`leak_lib`·`core_warn` 이 오히려 올랐다. 다만 같은 도메인 문서가 늘면 상위 슬롯
경쟁이 심해져 소폭의 밀어내기는 남는다(제외 없는 기준으로 재면 0.881→0.833). ⚠ **평가셋 42건에는
새로 들어온 120행을 정답으로 하는 질의가 하나도 없다** — 이득은 측정되지 않고 손실만 측정된다는 뜻이다.
새 코퍼스를 넣을 때는 **그 코퍼스를 겨냥한 질의도 함께** 넣어야 판정이 성립한다.

## 2026-08-29 — e5-small / 350자 청크 / 834청크(365 문서)

| k | Recall@1 | Recall@k | MRR |
|---:|---:|---:|---:|
| 3 | 0.476 | 0.690 | 0.583 |
| 5 | 0.476 | 0.786 | 0.606 |
| **10** | 0.476 | **0.881** | 0.620 |
| 15 | 0.476 | 0.905 | 0.622 |
| 20 | 0.476 | 0.929 | 0.623 |

**k=10 권장.** 이후 수확 체감이고 MRR 은 k=8 부터 평평하다. 청크가 350자라
10건을 주입해도 3,500자로 컨텍스트 예산(`rag.chunking.max-total-chars=8000`)에 여유가 있다.

소스별 Recall@1 / Recall@10:

| source | @1 | @10 |
|---|---:|---:|
| `oom_kind` | 7/8 | 8/8 |
| `mat_hint` | 4/5 | 5/5 |
| `csv` | 3/8 | 8/8 |
| `leak_lib` | 2/7 | 6/7 |
| `core_warn` | 3/6 | 5/6 |
| `leak_fb` | 1/5 | 3/5 |
| `vendor` | 0/3 | 2/3 |

## 왜 Recall@1 이 0.476 인가

e5 는 코사인 값이 **0.86~0.91 좁은 띠**에 몰린다. 한 도메인에 365 문서가 있으면
상위 후보들의 점수 차이가 0.01 안팎이라 1위 자리는 사실상 종이 한 장 차이다
(실측: `vendor:WEBLOGIC` 이 6위인데 1위와 0.0113 차이). RAG 는 상위 k 건을
프롬프트에 넣으므로 **Recall@k 가 실질 지표**이고, Recall@1 을 올리려면
리랭커(cross-encoder)가 필요한데 이 서버의 RAM 예산으로는 무리다.

## 평가셋의 한계 (해석 시 감안할 것)

- 42건 전부 **작성자가 문서에서 유도한 질문**이다. 정답이 구성상 확정이라
  라벨 편향은 없지만, 실제 운영자의 어휘 분포와는 다를 수 있다.
- 정답을 문서 1건(2건은 2개)으로 좁게 잡았다. 실제로는 여러 문서가 타당한
  답인 경우가 있어 **Recall@1 은 실제 품질을 과소평가**한다.
  (예: "DirectByteBuffer 가 회수되지 않습니다" 에 `csv:oom-2026-004`
  "Direct ByteBuffer / off-heap OOM" 이 나오는 것은 오답이 아니다.)
- `ai_chat` 에서 실제 질문을 뽑으려 했으나 31건 중 쓸 만한 것이 22건이고
  난이도가 낮아 평가셋으로는 약했다. 실제 운영 질의가 쌓이면 교체할 것.

## 변경 이력

| 시점 | 조건 | Recall@5 | 비고 |
|---|---|---:|---|
| 초기 | MiniLM(패러프레이즈), 청킹 없음 | — | 10개 표본 1위 적중 4/10 |
| 모델 교체 | e5-small, 350자 청킹 | 0.810 | 1위 적중 7/10 |
| 고아 청크 수정 | 최소 150자 보장 | 0.786 | `mat_hint` 2/5→4/5, `oom_kind` 5/8→7/8 |
